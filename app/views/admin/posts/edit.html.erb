<% title "Edit post" %>

<%= simple_form_for @post, url: admin_post_url(@post.url), method: :put do |f| %>
	<%= f.input :title %>
	<%= f.input :link, label: 'Link (optional)' %>
	<%= f.input :body, :as => :text %>
	
	<%= f.submit 'Save' %>
	<%= link_to 'Preview', '#', id: 'preview' %>
<% end %>