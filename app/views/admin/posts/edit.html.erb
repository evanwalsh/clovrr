<% title "Edit post - #{@post.title}" %>

<%= simple_form_for @post, url: admin_posts_url, html_options: { method: :put } do |f| %>
	<%= f.input :title %>
	<%= f.input :link, label: 'Link (optional)' %>
	<%= f.input :body, :as => :text %>
	
	<%= f.submit 'Save' %>
<% end %>