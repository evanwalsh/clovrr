<% title "New post" %>

<%= simple_form_for @post, url: admin_posts_url do |f| %>
	<%= f.input :title %>
	<%= f.input :link, label: 'Link (optional)' %>
	<%= f.input :body, :as => :text %>
	
	<%= f.submit 'Publish' %>
<% end %>