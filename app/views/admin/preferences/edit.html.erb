<% title "Edit preference" %>

<%= simple_form_for @preference, url: admin_preference_url(@preference.name), method: :put do |f| %>
	<%= f.input :name %>
	<%= f.input :value %>
	
	<%= f.submit 'Save' %>
<% end %>