<% title "New user" %>

<%= simple_form_for @user, url: admin_users_url do |f| %>
	<%= f.input :username %>
	<%= f.input :email %>
	<%= f.input :password %>
	<%= f.input :admin, as: :boolean %>
	
	<%= f.submit 'Save' %>
<% end %>